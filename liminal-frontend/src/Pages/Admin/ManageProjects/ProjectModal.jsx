/* eslint-disable react/prop-types */

const ProjectModal = ({projectData}) => {    
    return (
        <div>
            Project Modal {projectData._id}
        </div>
    );
};

export default ProjectModal;